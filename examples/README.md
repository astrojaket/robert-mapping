# Examples

`hammond_wasp43b.yml` points to the small NumPy arrays in the repository and
is the first configuration to validate. `minimal.yml` is a compact template
for a new data set. Paths are relative to each YAML file.

